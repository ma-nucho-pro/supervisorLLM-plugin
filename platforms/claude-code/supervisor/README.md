# Supervisor for Claude Code

Native Claude Code adapter. Includes the universal Skill, 27 agents, prompt injection, and an independent `Stop` agent judge.

Load locally:

```bash
claude --plugin-dir .
```

Invoke explicitly:

```text
/supervisor:supervisor <task>
```
