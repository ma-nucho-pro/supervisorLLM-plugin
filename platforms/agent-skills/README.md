# Generic Agent Skills hosts

Use this for any harness that implements the open Agent Skills `SKILL.md` format but has no Supervisor-native adapter in this repository. Copy or install the `supervisor/` directory into that harness's Agent Skills location. The workflow remains portable; hard lifecycle hooks and native subagent definitions are host-specific and are not claimed for unknown harnesses.
